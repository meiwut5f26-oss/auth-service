# Copy to .env for local (non-Docker) runs.
# Spring loads this file via spring.config.import in application.yml.

# App
PORT=8080

# Database
DATABASE_URL=jdbc:postgresql://localhost:5432/csfc_auth
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Mail
MAIL_USERNAME=your-email@domain.com
MAIL_PASSWORD=your-mail-app-password

# JWT
JWT_SECRET=replace-with-a-long-random-secret

# Bootstrap admin account
ADMIN_INIT_ENABLED=true
ADMIN_INIT_EMAIL=admin@csfc.local
ADMIN_INIT_PASSWORD=ChangeMe@123
ADMIN_INIT_NAME=System Admin
ADMIN_INIT_ROLE=ADMIN

