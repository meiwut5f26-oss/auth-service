# Deprecated: use the single .env file at project root instead.
# This file previously contained Docker-specific variables. We now standardize on a single `.env`.
# Copy .env.example -> .env for local development and Docker Compose.
# Example: Copy values from `.env.example` or edit `.env` directly.
# DO NOT commit real secrets into the repository.

# Use the single .env file in the project root for all environment variables.

